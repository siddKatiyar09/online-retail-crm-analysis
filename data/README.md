# Data Folder

Place the dataset file here before running the project:

- `OnlineRetail.csv`

The dataset is intentionally not committed to GitHub to keep the repository lightweight and clean.

After adding the file, you can run:

```powershell
python src/analyze_online_retail.py
```
